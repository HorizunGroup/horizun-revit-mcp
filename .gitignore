bin/
obj/
node_modules/
*.user
.vs/
build/
artifacts/
dist/

# Per-job NuGet cache used by the release workflow inside the checkout.
# It is build infrastructure state, never source provenance.
.horizun-nuget-*/

# Python doc-generation tooling and its output — unrelated to the C# MCP source.
# Kept out of the tree rather than deleted; move them elsewhere if you prefer.
__pycache__/
*.pyc
build_horizun_jr_*.py
deliverables/
docx_render/

# Local visual/inspection artifacts produced while checking the committed QA/QC template.
examples/qaqc-report-template-preview.png
examples/qaqc-report-template.xlsx.inspect.ndjson
